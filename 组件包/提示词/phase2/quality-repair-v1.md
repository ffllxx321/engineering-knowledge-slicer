# Phase 2 候选格式修复 v1

只修复给定批次的格式或合同错误，允许返回 `{"items":[]}`。

- 不新增候选，不补全缺失事实，不改写证据含义。
- 每条逐字证据必须完整存在于其指定块中。
- 不跨块、表格行、邮件消息或附件搬运证据。
- 只使用允许的业务条目类型；文档目录不是条目类型。
- 无法在原输入内修好的候选应删除，不要猜测。
- 输出只含符合合同的 JSON 对象，不附解释。
