{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "engineering-knowledge-slicer://schema/parse-package-1.1",
  "type": "object",
  "additionalProperties": false,
  "required": ["source_path", "source_hash", "source_type", "parser", "language", "markdown", "pages", "quality", "schema_version"],
  "properties": {
    "source_path": { "type": "string", "minLength": 1 },
    "source_hash": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "source_type": { "type": "string", "minLength": 1 },
    "parser": { "enum": ["mineru-api", "paddleocr-api", "text-normalizer", "eml-parser"] },
    "parser_model": { "type": "string" },
    "remote_job_id": { "type": "string" },
    "language": { "enum": ["zh", "en", "ja", "mixed", "unknown"] },
    "markdown": { "type": "string" },
    "pages": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["page", "text"],
        "properties": {
          "page": { "type": "integer", "minimum": 1 },
          "text": { "type": "string" },
          "span_ids": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "provenance": {
      "type": "object",
      "required": ["version", "spans"],
      "properties": {
        "version": { "type": "string" },
        "spans": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["span_id", "start", "end", "text", "text_hash"],
            "properties": {
              "span_id": { "type": "string" },
              "page": { "type": "integer", "minimum": 1 },
              "block_id": { "type": "string" },
              "line_id": { "type": "string" },
              "bbox": { "type": "array", "items": { "type": "number" }, "minItems": 4 },
              "start": { "type": "integer", "minimum": 0 },
              "end": { "type": "integer", "minimum": 0 },
              "text": { "type": "string" },
              "text_hash": { "type": "string" }
            }
          }
        }
      }
    },
    "images": { "type": "array", "items": { "type": "object" } },
    "quality": {
      "type": "object",
      "required": ["readable", "score", "components"],
      "properties": {
        "readable": { "type": "boolean" },
        "score": { "type": "number", "minimum": 0, "maximum": 1 },
        "components": { "type": "object" }
      }
    },
    "schema_version": { "const": "1.1" }
  }
}
